const fs = require('node:fs')
const { resolveFile } = require('./translate.js')

func bsdCommand(file):
  const filePath = resolveFile(file)
  fs.unlinkSync(filePath)
  print(`Deleted ${file}`)
end

module.exports = { bsdCommand }
